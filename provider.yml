eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
   rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3333/school?useUnicode=true&characterEncoding=utf8
    username: root
    password: 123
management:
  security:
    enabled: false
